{
  "name": "quarterly-demo",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "start": "serve docs -l $PORT",
    "dev": "serve docs -l 3000",
    "build": "echo 'Static site — no build step'",
    "test": "echo 'No tests configured'"
  },
  "dependencies": {
    "serve": "^14.2.4"
  }
}
