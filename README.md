# Contoso Project Dashboard

A static project management dashboard built with HTML and CSS.

## Quick start

```bash
npm install
npm run dev     # → http://localhost:3000
```

## Structure

- `docs/index.html` — Dashboard page
- `docs/theme.css` — Theme variables (colors, fonts, spacing)
- `docs/styles.css` — Component styles
